<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fanxl.admin.dao.GuidePriceDao">

    <!--批量创建-->
    <insert id="saveList" parameterType="List">
        insert into admin_guide_price (food, price_date, category_code, average_price, guide_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.food}, #{item.priceDate}, #{item.categoryCode}, #{item.averagePrice}, #{item.guidePrice})
        </foreach>;
    </insert>

    <!--获取上一次的价格日期的价格-->
    <select id="getLastGuidePrice" parameterType="Date" resultType="GuidePrice">
        select food, max_price, low_price
        from admin_guide_price
        where price_date =#{lastPriceDate};
    </select>

    <!--获取上一次的价格日期-->
    <select id="getLastPriceDate" resultType="Date">
        select price_date
        from admin_guide_price
        order by price_date desc
        limit 1;
    </select>

    <!--根据分类获取指导价-->
    <select id="list" parameterType="String" resultType="com.fanxl.admin.vo.GuidePriceVO">
        SELECT a.`average_price`, a.guide_price, a.food as name
        FROM `admin_guide_price` a
        WHERE a.category_code =#{categoryCode}
    </select>

    <!--获取所有指导价-->
    <select id="all" resultType="com.fanxl.admin.vo.GuidePriceVO">
        SELECT a.id, a.`average_price`, a.`guide_price`, a.food as name, a.category_code
        FROM `admin_guide_price` a
    </select>

    <delete id="deleteAll">
        delete from admin_guide_price;
    </delete>

</mapper>