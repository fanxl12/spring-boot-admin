<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fanxl.admin.dao.TodayPriceDao">

    <!--批量创建-->
    <insert id="saveList" parameterType="List">
        delete from admin_today_price;
        insert into admin_today_price (booth, food, price_date, price, special_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.booth}, #{item.food}, #{item.priceDate}, #{item.price}, #{item.specialPrice})
        </foreach>;
    </insert>

    <!--获取今日特价-->
    <select id="getAll" resultType="com.fanxl.admin.vo.TodayPriceVO">
        select a.id, a.price, a.special_price, a.food as foodName, a.booth as boothName, a.`price_date`
        from `admin_today_price` a
    </select>

</mapper>